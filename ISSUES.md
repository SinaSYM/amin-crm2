# لیست باگ‌ها و ناقص‌ها — فاز ۱ (Discovery)

تاریخ تهیه: 2026-07-03. این لیست حاصل بررسی فقط-خواندنی است؛ هیچ کدی تغییر نکرده. اولویت‌ها تخمینی‌اند؛ بررسی امنیتی کامل (فاز ۲) عمیق‌تر خواهد بود — مواردی که این‌جا برچسب امنیتی خورده‌اند، پیش‌نمایش همان فاز هستند.

## Critical

### C1 — `enrollments/[id]/payments/route.ts` هیچ auth ندارد
فایل کاملاً بدون `getSession`/چک نقش است (خط ۱–۱۷۳). هر کسی بدون لاگین می‌تواند اطلاعات enrollment+دانشجو+پرداخت‌های هر `id` را بخواند (GET) و طرح اقساط جدید برایش بسازد (POST). IDOR کامل + write غیرمجاز.

### C2 — رمزهای عبور هاردکد در اسکریپت‌های ریپازیتوری
- `scripts/reset-password.ts` (خط ۷–۸، ۱۷): ایمیل و رمز عبور واقعی یک کاربر مشخص هاردکد شده و رمز جدید در کنسول لاگ می‌شود.
- `scripts/bootstrap-admin.ts` (خط ۸–۹): در صورت نبود `ADMIN_EMAIL`/`ADMIN_PASSWORD`، ایمیل/رمز پیش‌فرض هاردکد استفاده می‌شود.

این‌ها credential نشت‌شده در تاریخچه‌ی گیت محسوب می‌شوند، حتی اگر اسکریپت به‌ندرت اجرا شود. نیاز به rotation و حذف از کد در فاز ۳.

### C3 — تأییدشده: فرم عمومی "درخواست مشاوره" روی صفحه ورود همیشه شکست می‌خورد (نه صرفاً یک ریسک)
`login-page.tsx:317-354` (`handleRegisterLead`) بدون هیچ کوکی/سشنی مستقیماً `POST /api/leads` را صدا می‌زند — این تب دقیقاً برای بازدیدکننده‌ی ناشناس روی صفحه‌ی لاگین طراحی شده (نیازمندی اصلی Phase 1: "API اتصال به وبسایت"). اما `leads/route.ts:128-131` بی‌قیدوشرط `getSession(request)` را چک می‌کند و بدون session خطای 401 برمی‌گرداند — یعنی این کاربر ناشناس هرگز حتی به مرحله‌ی اعتبارسنجی فیلدها هم نمی‌رسد.

**تأیید شده با `git log`**: این چک session در `POST /api/leads` از commit `80a317d` (۲۳ ژوئن ۲۰۲۶) وجود دارد — یعنی این مسیر از حدود ۱۰ روز پیش، مستقل از کار اخیر روی `next_followup_date`، همیشه برای این فرم عمومی 401 برگردانده و کار نکرده است. علاوه بر این، سه کامیت اخیرتر (`next_followup_date` اجباری، ۲۸ ژوئن–۱ ژوئیه) یک الزام دوم هم اضافه کرده‌اند که این فرم عمومی هرگز مقداری برایش نمی‌فرستد (`login-page.tsx:327-334` هیچ `next_followup_date` ندارد) — اما چون چک 401 زودتر در کد اجرا می‌شود، این فرم اصلاً به آن چک دوم نمی‌رسد.

این دقیقاً همان نوع «بخش ناقص ثبت‌نام/ورود» است که باید در فاز ۴ کامل و امن پیاده‌سازی شود: یا باید یک مسیر عمومی/بدون‌احراز مجزا برای لید‌های ورودی از وبسایت ساخته شود (با rate limiting و اعتبارسنجی مناسب برای جلوگیری از سوءاستفاده)، یا معماری فرم باید تغییر کند.

## High

### H1 — باگ سیستمیک scope اشتباه برای SALES_AGENT (۵ فایل، ۷ محل)
`enrollments/route.ts`, `enrollments/[id]/route.ts`, `payments/route.ts`, `payments/[id]/route.ts`, `tickets/route.ts`, `tickets/[id]/route.ts` همگی برای فیلتر دیدن داده توسط SALES_AGENT از رابطه‌ی `referred_by_id`/`referredLeads` استفاده می‌کنند — این رابطه‌ی افیلیت/معرفی است، نه رابطه‌ی مالکیت agent روی دانشجوی تبدیل‌شده (نگاه کنید PROJECT_OVERVIEW.md بخش ۵). نتیجه: یک agent می‌تواند enrollment/payment/ticket دانشجوی متعلق به agent دیگر را ببیند (اگر آن دانشجو تصادفاً لیدی را معرفی کرده باشد که به این agent اساین شده)، در حالی که از دیدن دانشجویانی که خودش تبدیل کرده محروم است. الگوی صحیح مرجع: `leads/route.ts` (`assigned_to_id`).

### H2 — بدون rate limiting روی login
نه در `login/route.ts` و نه به‌صورت میدل‌ور سراسری، هیچ محدودیت تلاش/backoff/lockout وجود ندارد. هم مسیر email+password و هم مسیر "quick login" (`userId`, وقتی DEMO_AUTH فعال است) قابل brute-force نامحدود هستند.

### H3 — بدون جریان بازیابی رمز عبور خودکار
دکمه "فراموشی رمز عبور" فقط toast نمایش می‌دهد (`login-page.tsx` خط ۳۶). تنها راه واقعی، اجرای دستی `scripts/reset-password.ts` توسط ادمین است (که خودش C2 است).

### H4 — سیاست رمز عبور ضعیف
فقط `min(8)` روی طول رمز چک می‌شود (`signup/route.ts` خط ۳۹)، بدون الزام پیچیدگی (حروف بزرگ/کوچک/عدد/نماد) یا چک لیست رمزهای رایج.

### H5 — کانال زمانی برای شمارش کاربر (timing side-channel)
در `loginWithCredentials()` (`auth.ts` خط ۱۶۴–۱۶۷)، اگر ایمیل وجود نداشته باشد فوراً return می‌شود، ولی اگر ایمیل وجود داشته باشد و رمز غلط باشد محاسبه‌ی کند scrypt اجرا می‌شود — این تفاوت زمانی می‌تواند برای حدس زدن اینکه یک ایمیل ثبت‌نام شده یا نه استفاده شود (پیام خطا خودش نشتی ندارد، فقط تایمینگ).

## Medium

### M1 — `ai/coach` و `ai/generate` بدون scope مالکیت روی `lead_id`
هر کاربر لاگین‌شده با نقش مجاز می‌تواند با هر `lead_id` دلخواه این دو endpoint را صدا بزند و یادداشت/تعامل لید متعلق به agent یا دپارتمان دیگر را ببیند — بدون چک دپارتمان/مالکیت (`ai/coach/route.ts` خط ۳۱–۴۴، `ai/generate/route.ts` خط ۳۱–۴۰).

### M2 — export endpointها دپارتمان‌اسکوپ نیستند
`enrollments/export/route.ts` و `users/export/route.ts` بدون فیلتر دپارتمان کل داده را CSV می‌دهند، در حالی که routeهای GET معادل‌شان (`enrollments/route.ts`, `users/route.ts`) درست دپارتمان را اسکوپ می‌کنند. یک DEPT_MANAGER/SALES_MANAGER می‌تواند از طریق export داده‌ی کل سازمان را dump کند.

### M3 — `tickets/route.ts` GET بدون فیلتر دپارتمان برای managerها
برخلاف بقیه‌ی موارد، manager همه‌ی تیکت‌های سازمان را می‌بیند، نه فقط دپارتمان خودش — ناسازگار با کانونشن پروژه.

### M4 — `isApproved` در schema پیش‌فرض `true` است
`prisma/schema.prisma` خط ۶۵: `isApproved Boolean @default(true)`. با اینکه امروز هیچ مسیر کد از این پیش‌فرض سوءاستفاده نمی‌کند (همه جا صراحتاً مقدار می‌دهند)، این پیش‌فرض با نیت "تأیید لازم دارد" همخوانی ندارد و یک منبع باگ آینده است اگر کسی مسیر ساخت User جدید اضافه کند بدون ست کردن صریح این فیلد.

### M5 — مدیریت session ضعیف در جزئیات
جدول `Session` فیلد `createdAt`/`lastUsedAt`/IP/User-Agent ندارد (بدون امکان ردیابی/ابطال انتخابی)، TTL ثابت ۳۰ روزه بدون sliding expiration، و بدون CSRF token مجزا (تکیه‌ی کامل روی `SameSite=Lax`).

### M6 — بدون UI کارکنان برای تیکت‌ها
Backend/API تیکت‌ها کامل است ولی `'tickets'` اصلاً در union نوع `ActiveView` (`store.ts`) نیست — کارمندان راهی برای دیدن/رسیدگی به تیکت‌ها در پنل ندارند، فقط دانشجو می‌تواند تیکت بسازد.

### M7 — سیستم affiliate/wallet بدون مکانیزم برداشت
موجودی کیف‌پول فقط از طریق تبدیل لید افزایش می‌یابد (`leads/[id]/convert/route.ts`)؛ هیچ endpoint برداشت/تسویه‌ای در کل پروژه وجود ندارد.

### M8 — قابلیت‌های AI بی‌صدا mock می‌شوند
وقتی `GEMINI_API_KEY` تنظیم نباشد (که در `.env` فعلی چنین است)، `gemini.ts` بدون هیچ پیام/نشانه‌ای به کاربر نهایی، پاسخ‌های canned فارسی برمی‌گرداند. کاربر فکر می‌کند این پاسخ واقعی هوش مصنوعی است.

## Low

### L1 — `logActivity` جا افتاده در چندین route جهش‌دهنده
`courses` (PUT/DELETE/POST)، `interactions` (PUT/DELETE/POST)، `tasks` (PATCH/POST)، `tickets` (PATCH/POST)، `leads/compute-scores`، `leads/import`، `enrollments/[id]/payments` POST — هیچ‌کدام audit trail ثبت نمی‌کنند، برخلاف کانونشن مستندشده در CLAUDE.md.

### L2 — کد مرده / بلااستفاده
- `src/app/api/route.ts` — stub "Hello, world!" بدون caller.
- `src/app/api/leads/[id]/assign/route.ts` — هیچ فراخوانی از فرانت‌اند ندارد (جایگزین شده با `PUT /api/leads/[id]`).
- شاخه‌ی `else` مخصوص SALES_AGENT در `analytics/route.ts` (خط ۶۵–۸۲) قابل دسترس نیست چون `isAuthorized` قبلاً همین نقش را رد کرده — کپی‌پیست باقی‌مانده از `dashboard/route.ts`.
- مدل Prisma `Account` (فرمت NextAuth) بلااستفاده در schema.
- dependency `next-auth` نصب شده ولی هیچ‌جا استفاده نمی‌شود.

### L3 — `classes` بدون endpoint ساخت (POST)
فقط GET/PUT/DELETE موجود است؛ کلاس جدید از طریق اپ قابل ساخت نیست.

### L4 — `leads/import` دپارتمان/agent را اتوماتیک ست نمی‌کند
برخلاف ساخت دستی لید (`leads/route.ts` POST خط ۱۹۷–۲۰۸)، ایمپورت CSV این فیلدها را خالی می‌گذارد — ناسازگاری رفتاری.

### L5 — چند نقطه `any` در تایپ‌اسکریپت
`student-portal.tsx` (خط ۷۷، ۹۱۲، ۹۶۳)، `sales-panel.tsx` (خط ۱۷۱)، `login-page.tsx` (خط ۱۱۸)، `users-page.tsx` (خط ۲۰۲، ۲۷۹).

### L6 — `worklog.md` منسوخ است
از Phase 7 به بعد به‌روزرسانی نشده، در حالی که حساس‌ترین کار پروژه (auth/RBAC) بعد از آن انجام شده — فقط در commit history و دو spec/plan محدود قابل ردیابی است.

### L7 — جزئیات ظاهری/cosmetic
- آیکون `Phone` روی فیلد ایمیل در فرم لاگین (`login-page.tsx` خط ۵۲۱) — باقی‌مانده از طراحی قدیمی‌تر مبتنی بر شماره تلفن.
- `affiliate/route.ts` خط ۹۱: آمار `clicks` جعلی (`totalReferred*3+5`) به‌عنوان داده‌ی واقعی نمایش داده می‌شود.
- `use-toast.ts` خط ۱۲: `TOAST_REMOVE_DELAY = 1000000` (~۱۶.۷ دقیقه) — quirk شناخته‌شده‌ی پیش‌فرض shadcn، بی‌ضرر.

---

## جمع‌بندی برای تصمیم شما

**C3 تأیید شد و در حال حاضر زنده و شکسته است** — مستقیماً به هدف اصلی شما ("تکمیل ثبت‌نام/ورود") مربوط می‌شود و پیشنهاد می‌کنم در اولویت اول فاز ۴ قرار بگیرد. بقیه‌ی موارد Critical/High (C1, C2, H1) کاملاً در حیطه فاز ۲ می‌گنجند و همان‌جا با severity رسمی گزارش می‌شوند.

منتظر تأیید شما برای شروع فاز ۲ هستم.
